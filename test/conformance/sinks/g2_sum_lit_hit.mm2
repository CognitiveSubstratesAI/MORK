; SumSink branch 1 (SIZES fixed literal, MATCHING): emits <result> iff sum == literal.
; Lone sink template => BTM root prefix is NON-EMPTY (= prefix(e)[5..] = "(correct) 6").
(fact a 1)
(fact b 2)
(fact c 3)
(exec 0 (, (fact $k $x)) (O (sum (correct) 6 $x)))
